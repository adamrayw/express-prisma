<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title><%= findNote.title %></title>
  </head>
  <body>
    <a href="/notes" style="text-align: left; margin-bottom: 20px">👈🏻 Back</a>
    <h1><%= findNote.title %></h1>
    <p class="date"><%= new Date(findNote.createdAt).toDateString() %></p>
    <p><%= findNote.content %></p>
    <form action="/note/<%= findNote.id %>/delete" method="POST">
      <button type="submit">Delete Note</button>
    </form>
  </body>
</html>

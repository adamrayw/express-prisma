<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Add New Note</title>
</head>
<body>
    <a href="/notes" style="text-align: left; margin-bottom: 20px;">👈🏻 Back</a>
    <h1>Add New Note</h1>

    <div class="form-container">
        <form action="/notes" method="POST">
            <div>
                <label for="title">
                    <input type="text" name="title" placeholder="Title">
                </label>
                <br>
                <small><%= msg.map(e => e.title) %> </small>
            </div>
            <div>
                <label for="content">
                    <textarea type="text" name="content" placeholder="Content" rows="10"></textarea>
                </label>
               <small><%= msg.map(e => e.content) %></small>
            </div>
            <div>
                <button type="submit">Save Note</button>
            </div>
        </form>
    </div>
</body>
</html>